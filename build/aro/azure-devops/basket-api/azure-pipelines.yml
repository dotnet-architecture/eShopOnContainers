variables:
  ProjectName: Basket.API
trigger:
  branches:
    include:
    - master
    - dev
  paths:
    include:
    - src/BuildingBlocks/*
    - src/Services/Basket/*
jobs:
- template: ../webapp-build.yml
  parameters:
    ProjectName: $(ProjectName)
